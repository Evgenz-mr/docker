server {
  listen 8080;
  server_name _;

  location = /health {
    access_log off;
    default_type text/plain;
    return 200 'ok\n';
  }

  location / {
    root /usr/share/nginx/html;
    try_files $uri $uri/ =404;
  }
}
